apiVersion: v1
kind: Service
metadata:
  name: zammad-railsserver
  namespace: zammad
spec:
  selector:
    app: zammad-railsserver
  ports:
  - protocol: TCP
    port: 9080        # The port that nginx expects to connect to
    targetPort: 9080  # The port the pod listens on
