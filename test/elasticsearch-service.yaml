apiVersion: v1
kind: Service
metadata:
  name: zammad-elasticsearch
  namespace: default
spec:
  ports:
    - port: 9200
      targetPort: 9200
  selector:
    app: zammad-elasticsearch
  type: ClusterIP